## RUSTFLAGS="-C target-feature=+simd128" wasm-pack build --release --target web
[package]
name = "hybridocr"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wee_alloc = "0.4.5"
argon2 = "0.5.3"
